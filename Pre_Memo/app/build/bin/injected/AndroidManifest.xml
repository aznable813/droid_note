<?xml version="1.0" encoding="utf-8"?>
<manifest
	xmlns:android="http://schemas.android.com/apk/res/android"
	package="com.Pre_Memo"
	android:versionCode="4"
	android:versionName="1.0.4">

	<uses-sdk
		android:minSdkVersion="14"
		android:targetSdkVersion="21"/>

	<uses-permission
		android:name="android.permission.READ_EXTERNAL_STORAGE"/>

	<uses-permission
		android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

	<uses-permission
		android:name="android.permission.INTERNET"/>

	<uses-permission
		android:name="android.permission.ACCESS_NETWORK_STATE"/>

	<application
		android:allowBackup="true"
		android:icon="@drawable/ic_memo_app512"
		android:label="@string/app_name"
		android:theme="@style/AppTheme">

		<activity
			android:name=".StartUp"
			android:label="@string/app_name">

			<intent-filter>

				<action
					android:name="android.intent.action.MAIN"/>

				<category
					android:name="android.intent.category.LAUNCHER"/>

			</intent-filter>

		</activity>

		<activity
			android:name=".list_view"
			android:label="@string/activity_list_v">

		</activity>

		<activity
			android:name=".MainActivity"
			android:label="@string/app_name">

		</activity>

		<activity
			android:name=".input_memo"
			android:label="@string/activity_list_memo"
			android:windowSoftInputMode="adjustResize">

		</activity>

		<activity
			android:name=".SettingsPrf"
			android:label="@string/activity_SettingsPrf">

		</activity>

		<activity
			android:name=".Garbage_Can_View"
			android:label="@string/activity_garbage">

		</activity>

		<activity
			android:name=".Garbage_Contents"
			android:label="@string/activity_garbage_Contents">

		</activity>

	</application>

</manifest>

